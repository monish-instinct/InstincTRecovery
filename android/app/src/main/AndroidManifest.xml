<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- BLE: Android 12+ uses BLUETOOTH_SCAN/CONNECT; older needs location for scan. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Location: FINE/COARSE are used for two things — (a) BLE scan on API <=30
         (legacy requirement), and (b) GPS workout-route tracking on all API levels
         (while-in-use). Because of (b) the FINE permission is NOT capped at API 30. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- FGS location type (API 34+): lets EdgeTrackingService keep sampling GPS while
         a run/ride/walk session is active and the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Camera: reading the barcode on a food packet in the food log, and
         nothing else. No frame is captured, stored or sent — mobile_scanner is
         asked for the digits only. Declared not-required so a phone without a
         camera still installs; the scan row falls back to typing. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Edge Tracking foreground service: keeps the strap connection alive in the background. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Band-gesture "ring my phone" haptic. Normal permission, no runtime prompt. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- The phone's OWN step counter (Sensor.TYPE_STEP_COUNTER), which is the app's
         only 24/7 step source — a wrist cannot resolve gait at 1 Hz. RUNTIME
         (dangerous) permission on API 29+; below that the sensor reads without it,
         which is why minSdk 26 still works. This is NOT a Play Console
         declaration-class permission, only a data-safety disclosure.

         The listener rides EdgeTrackingService's process (see PhoneStepCounter.kt) —
         Android 9+ withholds sensor events from background processes and a foreground
         service of ANY type lifts that. We deliberately do NOT claim the API 34
         "health" FGS type for it: the service's purpose is still the band connection,
         and each declared type costs a separate Play declaration plus a demo video.
         If Play ever pushes back, add FOREGROUND_SERVICE_HEALTH here and OR the type
         into EdgeTrackingService.onStartCommand — no code shape changes. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Reboot survival: restart EdgeTrackingService + headless auto-connect after device reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- CompanionDeviceManager: association makes us a "companion app" (exempt from the
         API 31+ background foreground-service-start restriction) and lets the OS relaunch
         us (EdgeCompanionService) when the associated band comes into range. -->
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_USE_DATA_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />
    <!-- Battery-optimization (Doze) exemption request — user-granted via the system
         dialog fired from the "Keep alive in background" row. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Keep the CPU alive while the boot receiver starts the foreground service. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- OTA self-update: download + launch the system installer for the new APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- QUERY_ALL_PACKAGES: declared for a notification-relay app picker that was
         never built. Nothing in lib/ or the Kotlin sources calls
         getInstalledApplications or queryIntentActivities, and the `installed_apps`
         package it was declared for has no Dart import. It is the most
         policy-expensive permission there is, so it goes — with node="remove" so a
         library manifest cannot merge it back in behind us. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:node="remove" />
    <!-- ── Health Connect (export to Google Health Connect) ──
         The `health` plugin treats a WRITE request as READ+WRITE (hasPermissions
         checks both), so the READ_* permissions MUST be declared too even though
         we only write. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_BASAL_METABOLIC_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_BASAL_METABOLIC_RATE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <!-- READ only, and no WRITE counterpart on purpose: body metrics are read
         INTO the local profile (weight drifts, and the calorie estimate
         depends on it) and this app has no business writing them back. Health
         Connect silently returns nothing for an undeclared permission rather
         than failing, so an omission here looks exactly like an empty store. -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_HEIGHT" />
    <!-- READ only, and there will never be a WRITE counterpart. These come
         from devices CLEARED to take them — a blood-pressure cuff, a CGM, a
         thermometer — and the whole value is that OpenStrap did not measure
         them. They are shown with the source app's name attached, they are
         never blended into anything of ours, and nothing derived takes one as
         an input. See lib/health/health_measurement_import.dart. -->
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <!-- Seeding a SHADOW resting-HR baseline from history the phone already
         holds (lib/health/health_rhr_seed.dart). READ_RESTING_HEART_RATE is
         already declared above for the write path. -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <!-- CompanionDeviceManager availability (not required: devices without it just skip
         the association and rely on the sticky service + watchdog). -->
    <uses-feature android:name="android.software.companion_device_setup"
        android:required="false" />

    <application
        android:label="InstincT Recovery"
        android:name=".EdgeApplication"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">
        <!-- ── ZERO-COLLECTION BY DEFAULT (privacy tier 1) ──
             Every Firebase SDK auto-starts collecting the moment the app
             process does: Analytics logs first_open/session_start, Performance
             opens app-start + network traces, Crashlytics uploads any startup
             crash. All of that happens BEFORE Dart has read the user's stored
             telemetry consent (an async SharedPreferences load off the startup
             critical path), so relying on the runtime setters alone meant a
             fresh install collected before consent existed.

             These three flags turn the SDKs' auto-collection OFF at process
             start. They are the "enabled" (not "deactivated") variants, so
             TelemetryService can still switch collection ON at runtime once —
             and only once — affirmative consent has been loaded. Do NOT flip
             any of these to true: consent is the only thing allowed to. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_performance_collection_enabled"
            android:value="false" />
        <!-- No ad/measurement signals, ever — nothing here is consent-gated
             back on. -->
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect: permission-rationale entry point (required). -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Health Connect rationale for Android 14+ (VIEW_PERMISSION_USAGE). -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- Edge Tracking foreground service (background BLE drain). -->
        <service
            android:name=".EdgeTrackingService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <!-- CompanionDeviceService (API 31+): the OS binds this when the associated band
             comes into range (startObservingDevicePresence) — onDeviceAppeared restarts
             the tracking service, so background capture recovers even after a process
             kill. Never bound below API 31 (the action doesn't exist there). -->
        <service
            android:name=".EdgeCompanionService"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE">
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceService" />
            </intent-filter>
        </service>

        <!-- Boot receiver: restarts EdgeTrackingService + headless auto-connect after reboot.
             Handles BOOT_COMPLETED + QUICKBOOT_POWERON. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Tasker integration: receive Broadcast intents from Tasker (or any app)
             to buzz the WHOOP strap. Exported with no manifest permission (a
             signature permission would block Tasker itself) — TaskerReceiver
             instead requires a String extra "token" matching the per-install
             secret shown in Settings -> Automation, plus a short rate limit;
             anything else is silently dropped. Optional int extra "pattern"
             (default 2). -->
        <receiver
            android:name=".TaskerReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="wtf.openstrap.openstrap_edge.BUZZ_STRAP" />
            </intent-filter>
        </receiver>

        <!-- Notification relay (notification_listener_service plugin). The system binds
             this to deliver posted notifications; gated behind the user granting
             "Notification access" in system settings. Class lives in the plugin. -->
        <service
            android:name="notification.listener.service.NotificationListener"
            android:label="OpenStrap notification relay"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- OTA self-update (ota_update 7.x). The plugin's LIBRARY manifest only
             declares permissions — the host app MUST declare this FileProvider +
             receiver itself, or the install handoff throws "Couldn't find meta-data
             for provider with authority ...ota_update_provider" and crashes at 100%. -->
        <provider
            android:name="sk.fourq.otaupdate.OtaUpdateFileProvider"
            android:authorities="${applicationId}.ota_update_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
        <receiver
            android:name="sk.fourq.otaupdate.InstallResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="${applicationId}.ACTION_INSTALL_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widgets (Android siblings of ios/OpenStrapWidget). The app
             pushes snapshots via home_widget (WidgetService); these just render. -->
        <receiver
            android:name=".OpenStrapWidgetProvider"
            android:exported="true"
            android:label="@string/widget_openstrap_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_openstrap_info" />
        </receiver>
        <receiver
            android:name=".SleepWidgetProvider"
            android:exported="true"
            android:label="@string/widget_sleep_picker_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_sleep_info" />
        </receiver>
        <receiver
            android:name=".OvernightWidgetProvider"
            android:exported="true"
            android:label="@string/widget_overnight_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_overnight_info" />
        </receiver>
        <receiver
            android:name=".OpenStrapBatteryWidgetProvider"
            android:exported="true"
            android:label="@string/widget_battery_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_band_battery_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Health Connect app discovery (for install/status checks). -->
        <package android:name="com.google.android.apps.healthdata" />
    </queries>
</manifest>
