{
  "note": "SYNTHETIC. Every value below is generated by the formula in its own field, not measured. Not a capture, not anyone's physiology. See test/two_device_fixture_test.dart, which is the actual generator — this file is the documented spec it must match.",
  "day": "2020-01-02",
  "tz": "UTC",
  "devices": [
    {
      "device_id": "",
      "adapter": "gen4",
      "family": "gen4",
      "remote_id": "00:00:5E:00:53:00",
      "note": "RFC 7042 documentation MAC. The primary band; source IS NULL."
    },
    {
      "device_id": "oura-a1b2c3d4",
      "adapter": "oura",
      "family": "oura",
      "remote_id": "00:00:5E:00:53:01",
      "note": "RFC 7042 documentation MAC. source == 'oura' in production; NULL here only because commitSyncBatch never writes source (that column is set by oura_link._commit, which this fixture bypasses on purpose to exercise the unit-mixing branch)."
    }
  ],
  "regions": [
    { "id": "A", "start": "00:00:00", "end": "01:59:59", "devices": ["gen4"], "note": "gen4 only, contiguous -> single-owner span" },
    { "id": "B", "start": "02:00:00", "end": "03:59:59", "devices": ["gen4", "oura"], "note": "the collision case: two device_ids at one rec_ts, every second" },
    { "id": "C", "start": "04:00:00", "end": "04:00:04", "devices": [], "note": "a REAL gap (d == null)" },
    { "id": "D", "start": "04:00:05", "end": "05:59:59", "devices": ["oura"], "note": "oura only, fallthrough, >= 3 buckets" },
    { "id": "E", "start": "06:00:00", "end": "06:00:01", "devices": ["gen4"], "note": "gen4 present for 2 seconds only -- BELOW the hysteresis threshold: must NOT become a hand-back (M3+ concern; M0 only asserts this region decodes cleanly)" },
    { "id": "F", "start": "06:00:02", "end": "07:59:59", "devices": ["gen4"], "holes": ["06:30:00", "07:15:00"], "note": "gen4 only, with 2 one-second holes -- a gap INSIDE an owned span is not a fallthrough (M3+ concern)" }
  ],
  "formulas": {
    "gen4_hr": "60 + (rec_ts % 7)",
    "gen4_rr_ms": "1000 - (rec_ts % 5) * 10, one beat per second",
    "gen4_accel": "ax=0.0, ay=0.0, az=1.0 constant (a still wrist)",
    "gen4_skin_temp_raw": "30000 + (rec_ts % 11)",
    "oura_skin_temp_c": "33.0 + (rec_ts % 9) / 10 -- THE OTHER UNIT",
    "oura_hr_accel": "absent (NULL), matching OuraAdapter.signals == {}"
  },
  "load_bearing": "Region B's oura skin_temp_c against gen4 skin_temp_raw is the unit-mixing case (spec-m0-m2.md §5) -- a regression gate for M0, not only for M5."
}
